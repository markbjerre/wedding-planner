# Supabase cloud sync — copy to .env.local (gitignored). Project ref in dashboard URL.
# Table + RLS: supabase/001_wedding_planner_layouts.sql (also applied via Supabase migrations).
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=
